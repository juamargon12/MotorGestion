<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MotorGestion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MotorGestion" parent="Base.Theme.MotorGestion" />

    <!-- Estilo para los campos de texto del Login -->
    <style name="InputEstilizado" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/colorPrincipal</item>
        <item name="hintTextColor">@color/colorPrincipal</item>
    </style>

    <!-- Estilo para el botón principal -->
    <style name="BotonEstilizado" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/colorPrincipal</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">8dp</item>
    </style>

</resources>