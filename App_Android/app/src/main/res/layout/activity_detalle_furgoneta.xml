<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Foto del vehículo (Tema 05 — cámara + Base64) -->
        <ImageView
            android:id="@+id/imgFoto"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:background="#EEEEEE"
            android:contentDescription="Foto de la furgoneta"
            android:layout_marginBottom="8dp" />

        <!-- Campos editables -->
        <EditText
            android:id="@+id/etModelo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Nombre / Modelo" />

        <EditText
            android:id="@+id/etMatricula"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Matrícula" />

        <EditText
            android:id="@+id/etCombustible"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Combustible (Diesel/Gasolina/Electrico/Hibrido)" />

        <EditText
            android:id="@+id/etCarga"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Carga Máxima (Kg)"
            android:inputType="numberDecimal" />

        <EditText
            android:id="@+id/etZona"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Zona" />

        <!-- Botón de cámara -->
        <Button
            android:id="@+id/btnFoto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📷 Tomar Foto"
            android:layout_marginTop="16dp" />

        <!-- Botón guardar cambios (PUT general) -->
        <Button
            android:id="@+id/btnGuardar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="💾 Guardar Cambios"
            android:backgroundTint="#4CAF50"
            android:layout_marginTop="8dp" />

        <!-- Botón eliminar -->
        <Button
            android:id="@+id/btnEliminar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Eliminar Furgoneta"
            android:backgroundTint="#FF0000"
            android:layout_marginTop="8dp" />
    </LinearLayout>
</ScrollView>
